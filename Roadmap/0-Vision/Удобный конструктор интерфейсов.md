---
title:
track: FE
horizon: 2024-2026
tags:
  - vision
---

> [!hint]- Памятка
> * `dependents` — ссылки на итерации, от которых зависит текущая.
> * `parent` — родительская итерация (как правило theme).
> * `horizon` — горизонт планирования (год, квартал и т.п.).
> * `track` — FE (FrontEnd), BE и т.п.
> * `title` — заголовок; если пусто, используется имя файла.

```dataviewjs
await dv.view("views/roadmap-iteration-summary", { dv });
```

## Описание

Прикладной разработчик собирает рабочее место из готовых компонентов без ручной вёрстки, а пользователь сам настраивает таблицы и представления под свою работу.

```dataviewjs
await dv.view("views/roadmap-vision-epics", { dv });
```

## Элементы
